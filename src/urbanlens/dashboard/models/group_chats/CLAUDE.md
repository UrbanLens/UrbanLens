# dashboard/models/group_chats/ — Goals (from docs/GOALS.md)

## Encryption

- Direct messages: end-to-end encrypted by default, as close to unconditionally enforced as
  possible — there should be no way to turn it off.
