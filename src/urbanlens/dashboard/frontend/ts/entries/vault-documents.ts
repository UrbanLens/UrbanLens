// Vault > Documents grid: infinite scroll, off-screen pruning, and sort. The
// module wires up its own DOMContentLoaded listener on import, so this entry
// only has to pull it in - see shared/vault-document-grid.ts.
import "../shared/vault-document-grid";
