// Vault > Photos grid: infinite scroll, off-screen pruning, and sort. The
// module wires up its own DOMContentLoaded listener on import, so this entry
// only has to pull it in - see shared/vault-photo-grid.ts.
import "../shared/vault-photo-grid";
