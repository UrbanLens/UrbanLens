import { installGlobalIconPicker } from "../shared/icon-picker";

// The saved-filter detail page (pages/pin_lists/saved_filter_detail.html) renders the shared _icon_picker.html partial.
installGlobalIconPicker();
